-- 할 일 목록 드래그 정렬용 컬럼 (2026-09-06)
-- 우선/보통 두 구간은 기존 pinned(0/1)로 나누고, 구간 안의 순서를 sort_order 로 잡는다.
-- 값은 화면 순서대로 10, 20, 30… 으로 다시 매긴다(간격을 둬서 사이에 끼워 넣기 쉽게).
-- NULL 인 행(아직 순서를 안 정한 것)은 정렬에서 뒤로 밀린 뒤 마감일·id 순으로 잡힌다.
ALTER TABLE todos ADD COLUMN sort_order INTEGER;
